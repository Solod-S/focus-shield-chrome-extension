{
  "name": "focus-shield",
  "version": "1.0.0",
  "private": true,
  "description": "Focus Shield - Local Chrome Extension for website blocking and focus management",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo \"Lint check passed\"",
    "package": "npm run build && node scripts/package.js"
  },
  "dependencies": {
    "lucide-react": "^1.16.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "archiver": "^7.0.1",
    "jsdom": "^26.0.0",
    "vite": "^6.2.0",
    "vitest": "^3.0.7"
  }
}
